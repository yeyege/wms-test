# ===== WMS Docker 环境变量示例 =====
# 复制为 .env 后执行 docker compose up -d 生效；不建 .env 时使用右侧默认值。
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=wms
MYSQL_USER=wms
MYSQL_PASSWORD=wms
# 后端容器内数据库连接由 compose 自动注入，本地开发如需切换数据库：
# DATABASE_URL=mysql+pymysql://wms:wms@localhost:3306/wms?charset=utf8mb4
